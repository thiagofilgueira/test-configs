server:
  port: 8765

eureka:
  instance:
    hostname: localhost
    port: 9091
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${eureka.instance.port}/eureka/
  server:
    wait-time-in-ms-when-sync-empty: 3000

spring:
  rabbitmq:
    dynamic: true
    host: localhost
    port: 5672
    username: mqadmin
    password: Admin123XX_delivery